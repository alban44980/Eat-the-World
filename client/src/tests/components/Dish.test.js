// import { render, screen } from '@testing-library/react';
// import CountryPage from '../components/Dish.js';

// jest.mock('../Services/ApiService.js', () => ({
//   getDishInfo: () => ({ dish: 'Paella' }),
// }));

//mockgetDishInfo
//check if the state changes to mock value
//async test
//getDishInfo should return smth

// {error: null,
// data: res.blablabla}

// {
//   error: err.message,
//   data: null
// }

//return an object instead in both cases

//move setDishInfo outside

// });

// describe('Dish component', () => {
//   test('Should match the spnapshot', () => {
//     const { container } = render(<CountryPage />);
//     expect(container.firstChild).toMatchSnapshot();
//   });
// test('Get Dish Info should return an array', () => {

// });

//expect this function to  has been called with the right prop
